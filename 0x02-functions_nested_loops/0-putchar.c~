#include "unistd.h"

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
  char *input = "Alx";

  while (*input)
    {
      _putchar(*input);
      input++;
    }
  _putchar('\n');

  return (0);
}
